{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",

      "//": "Raise this to Debug to see the middleware explain itself.",
      "//_2": "It logs the address of an unknown proxy when it declines",
      "//_3": "to process forwarded headers, which names exactly what",
      "//_4": "you are missing from TrustedProxies below.",
      "Microsoft.AspNetCore.HttpOverrides": "Information"
    }
  },

  "//TrustedProxies": [
    "Addresses IIS/Kestrel sees as the CONNECTION PEER, not the public",
    "address of your load balancer. Check what the app actually",
    "receives via the /_whoami endpoint in Program.cs before filling",
    "this in: on a dual-stack socket you may need the ::ffff: form."
  ],
  "TrustedProxies": [
    "10.0.0.5",
    "10.0.0.6"
  ],

  "AllowedHosts": "*"
}
